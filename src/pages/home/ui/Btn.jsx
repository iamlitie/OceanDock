import styled from 'styled-components'

import border from 'utils/border'

const Btn = border(
    styled.div `
        width: 100%;
        height: .32rem;
        background: #fff;
        text-align: center;
        line-height: .32rem;
        font-size: .16rem;
        color: #9b9b9b;
    `
)

export default Btn